@*@QnSCodeCopy*@
@model QnSCommunityCount.AspMvc.Models.Business.App.CommunityCosts
@{
    var controller = ViewContext.RouteData.Values["controller"].ToString();
    Func<string, string> translateFor = ViewData["TranslateFor"] as Func<string, string>;
}

<div class="form-group">
    <label class="control-label">@translateFor(nameof(Model.CostCollection.Designation))</label>
    <input asp-for="CostCollection.Designation" class="form-control" />
    <span asp-validation-for="CostCollection.Designation" class="text-danger"></span>
</div>
<div class="form-group">
    <label class="control-label">@translateFor(nameof(Model.CostCollection.Description))</label>
    <input asp-for="CostCollection.Description" class="form-control" />
    <span asp-validation-for="CostCollection.Description" class="text-danger"></span>
</div>
<div class="form-group">
    <label class="control-label">@translateFor(nameof(Model.CostCollection.Currency))</label>
    <input asp-for="CostCollection.Currency" class="form-control" />
    <span asp-validation-for="CostCollection.Currency" class="text-danger"></span>
</div>
<div class="form-group">
    <label class="control-label">@translateFor(nameof(Model.CostCollection.Category))</label>
    <input asp-for="CostCollection.Category" class="form-control" />
    <span asp-validation-for="CostCollection.Category" class="text-danger"></span>
</div>
<div class="form-group">
    <label class="control-label">@translateFor(nameof(Model.CostCollection.Members))</label>
    <input asp-for="CostCollection.Members" class="form-control" />
    <span asp-validation-for="CostCollection.Members" class="text-danger"></span>
</div>

@await Html.PartialAsync("_BalancesBody", Model)

@await Html.PartialAsync("_CostRecordsBody", Model)
